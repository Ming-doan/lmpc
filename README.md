# Language Model Platform Comparison

## Quick Start

```sh
# Clone repository
git clone https://github.com/Ming-doan/lmpc.git && cd ./lmpc
# Install dependencies
pip install poetry && poetry install
# Start evaluate
poetry run ./run.py --export-dir ./report
```